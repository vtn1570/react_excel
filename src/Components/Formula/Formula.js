import React from 'react' 
import classes from '../../scss/Formula.module.scss'

export const Formula = () => {
    return (
        <div className={classes.formula}>
            <div className={classes.info}>fx</div>
            <div></div>
        </div>

    )
}
